import sun.misc.unsafe;

public BadClient {
    public static void main (String[] args) {
        ArrayIntList list = new ArrayIntList();
        // TODO: Use unsafe to peek past at values past the end of the list
    }
}